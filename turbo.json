{
	"$schema": "https://turborepo.dev/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"inputs": ["src/**", "package.json", "tsconfig.json", "tsconfig.*.json", "build.ts", "tsdown.config.ts", "biome.json"],
			"outputs": ["dist/**"]
		},
		"test": {
			"dependsOn": [],
			"inputs": ["**/*.test.ts", "**/*.test.tsx", "vitest.config.ts"]
		},
		"check-types": {
			"dependsOn": [],
			"inputs": ["**/*.ts", "**/*.tsx", "!**/*.test.ts", "!**/*.test.tsx"]
		},
		"//#format-and-lint": {},
		"//#format-and-lint:fix": {
			"cache": false
		},
		"dev": {
			"persistent": true,
			"cache": false
		}
	}
}
